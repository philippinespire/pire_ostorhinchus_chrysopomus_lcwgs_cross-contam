# GenErode Symlink Listings

This directory contains captured `ls -lh` outputs from the GenErode raw reads symlinks directory on the HPC.
`generode_hist_symlinks.txt` covers historical samples and `generode_cont_symlinks.txt` covers contemporary samples.
Use these listings to verify that link IDs match the original FASTQ names as described in `OBJECTIVES.md`.
